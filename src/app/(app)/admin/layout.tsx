// This file is obsolete and has been neutralized.
// The main app layout now handles the admin sidebar and header.
export default function AdminLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
