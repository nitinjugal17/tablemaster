// This file is intentionally left blank.
// Types have been moved to the src/lib/types/ directory
// and are re-exported from src/lib/types/index.ts.
// Existing imports like `import type { ... } from '@/lib/types'`
// will now resolve to `src/lib/types/index.ts`.
// This file can be deleted if your build system and IDE correctly
// resolve directory imports to its index file without this placeholder.
// For safety during this refactor, it's kept to re-export.

export * from './types/index';
